{
  "name": "revoke-github-token-js",
  "version": "1.1.1",
  "description": "JavaScript module to revoke an ephemeral GitHub token, part of the fetch-github-token GH action",
  "main": "dist/index.cjs",
  "scripts": {
    "build": "npx @vercel/ncc build revoke.cjs -o dist && echo '' >> dist/index.cjs",
    "test": "node --experimental-vm-modules node_modules/.bin/jest",
    "verify-bundle": "npx @vercel/ncc build revoke.cjs -o dist-verify && diff -b -B dist dist-verify && rm -rf dist-verify"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@octokit/core": "^7.0.6"
  },
  "engines": {
    "node": ">=24"
  },
  "license": "MIT",
  "devDependencies": {
    "@vercel/ncc": "0.38.4",
    "jest": "30.3.0"
  }
}
