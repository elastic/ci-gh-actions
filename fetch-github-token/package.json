{
  "name": "fetch-github-token-js",
  "version": "1.0.0",
  "description": "GitHub Action to fetch ephemeral GitHub token from Vault using OIDC authentication",
  "main": "dist/main.js",
  "scripts": {
    "build": "ncc build revoke.cjs -o dist",
    "test": "node --experimental-vm-modules node_modules/.bin/jest",
    "prepare": "husky",
    "verify-bundle": "ncc build revoke.cjs -o dist-verify && diff -r dist dist-verify && rm -rf dist-verify"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@octokit/core": "^7.0.6"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "@vercel/ncc": "^0.38.4",
    "husky": "^9.1.7",
    "jest": "^30.2.0"
  }
}
